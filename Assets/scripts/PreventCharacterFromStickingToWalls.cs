using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// add to a child object of a character (can be empty game object with 2D collider)
public class PreventCharacterFromStickingToWalls : MonoBehaviour {
	// this would go on the character moving around and get the 2D collider that
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
